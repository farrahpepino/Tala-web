// const mongoose = require('mongoose');

// const friendsSchema = new mongoose.Schema({
//     user1: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, // One user in the friendship
//     user2: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, // Other user in the friendship
//     createdAt: { type: Date, default: Date.now }, // When the friendship was created
// });

// const Friend = mongoose.model('Friend', friendsSchema);
// module.exports = Friend;
